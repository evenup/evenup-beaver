[Unit]
Description=Beaver log muncher for logstash

[Service]
ExecStart=/usr/local/bin/beaver -c /etc/beaver/beaver.conf -t redis -l /var/log/beaver.log $BEAVER_OPTIONS
Restart=on-failure
KillMode=process
